# LLMA4SE 2026 · Workshop 4 — requirements
# Target platform: free Google Colab (T4 GPU).
# Colab already ships torch + CUDA — do NOT reinstall torch there.
# The notebooks install all of this themselves in their setup cells;
# this file exists for local/venv runs and for reference.

# ── Agent brain (Hugging Face runtime for Qwen2.5-Coder-1.5B-Instruct) ──
transformers>=4.44
accelerate>=0.33

# ── Deterministic "eyes" (static analysis tools) ──
radon>=6.0.1                  # cyclomatic complexity + maintainability index
pylint>=3.2                   # classic linter (JSON output used by the agent)
code-quality-analyzer>=0.2.2  # PyExamine — 49-metric smell detector (MSR 2025)
ml-code-smell-detector @ git+https://github.com/KarthikShivasankar/ml_smells_detector.git
                              # MLScent — 76 ML anti-pattern detectors (CAIN 2025)

# ── Verification & data ──
pytest>=8.0                   # QA agent gate 2 (behaviour tests, sandboxed)
pandas>=2.0                   # Part 3: scoring the TD classifier vs gold labels

# ── Part 4 (bonus): production-framework stack ──
langgraph>=1.2
langchain-ollama>=1.1
deepagents>=0.6
# Ollama itself is a system binary, not a pip package:
#   curl -fsSL https://ollama.com/install.sh | sh    (Linux/Colab)
#   then: ollama pull qwen2.5-coder:7b               (or :3b on slow connections)

# ── Local (non-Colab) runs only ──
# torch>=2.3        # uncomment locally; pick the right CUDA/CPU build from pytorch.org
# jupyter           # uncomment to run the notebooks locally
