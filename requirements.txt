# LLMA4SE 2026 · Workshop 4
# OpenRouter is the provider. The official openai package is the client.
# Notebook installs these in Part 0. This file is for local/venv runs.

radon>=6.0.1
pylint>=3.2
code-quality-analyzer>=0.2.2
ml-code-smell-detector>=0.1.2
pytest>=8.0
freezegun>=1.5
pandas>=2.0
openai>=1.40
langgraph>=1.2
deepagents>=0.7
langchain-openai>=0.3

# Credentials (not packages):
#   OPENROUTER_API_KEY=...
#   LLM_MODEL=openai/gpt-4o-mini   # any OpenRouter slug
