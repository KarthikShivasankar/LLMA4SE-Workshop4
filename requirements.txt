# LLMA4SE 2026 · Workshop 4 — requirements
# Target platform: Google Colab (CPU runtime is enough — the model lives behind the OpenAI API).
# The notebook installs all of this itself in its Part 0 setup cell;
# this file exists for local/venv runs and for reference.

# ── Deterministic "eyes" (static analysis tools) ──
radon>=6.0.1                  # cyclomatic complexity + maintainability index
pylint>=3.2                   # classic linter (JSON output used by the agent)
code-quality-analyzer>=0.2.2  # PyExamine — 49-metric smell detector (MSR 2025)
ml-code-smell-detector>=0.1.2 # MLScent — 76 ML anti-pattern detectors (CAIN 2025)

# ── Verification & data ──
pytest>=8.0                   # QA agent gate 2 (behaviour tests, sandboxed)
pandas>=2.0                   # Part 3: scoring the TD classifier vs gold labels

# ── Agent brain ──
openai>=1.40                  # the OpenAI SDK — every agent calls through one llm() helper

# ── Part 4 (bonus): production-framework stack ──
langgraph>=1.2
deepagents>=0.7
# langchain-openai            # installed by the Part 4 Deep Agents cell:
#                             # deepagents reaches models through LangChain and needs
#                             # the OpenAI binding, which it does not depend on itself.

# ── Credentials ──
# Not a package. Provide OPENAI_API_KEY via a .env file, Colab Secrets, or the
# notebook's hidden prompt. Optionally pin a model with OPENAI_MODEL=...
